PYR = [7 5];

BasePyramid = GLCME_DesiredBaseMaps(PYR,20); bs = size(BasePyramid);

%Find Factors
% Set Factor for that pyramid to twice the greatest value

% If two odd numbers then Multiply two Together, if this goes over a
% certain limit then just use the pyramid

